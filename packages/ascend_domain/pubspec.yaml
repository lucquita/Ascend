name: ascend_domain
description: >
  Dominio de Ascend: entidades, value objects, enums, contratos de repositorio
  y casos de uso. Dart puro â€” no conoce Flutter ni Firebase.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  ascend_core: ^0.1.0
  freezed_annotation: ^3.1.0
  meta: ^1.18.0

dev_dependencies:
  # TOPE DURO: build_runner >=2.15.3 exige analyzer >=13.3, que exige meta ^1.18.3.
  # El Flutter SDK 3.44.4 pinnea meta 1.18.0 -> 2.15.1 es la última usable.
  # Revisar al actualizar Flutter.
  build_runner: 2.15.1
  freezed: ^3.2.0
  test: ^1.26.4
