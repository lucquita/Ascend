name: ascend_core
description: >
  Núcleo transversal de Ascend: Result, Failure, utilidades, validadores y logging.
  Dart puro — sin Flutter, sin Firebase.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  # Pinneado por el Flutter SDK (flutter_test) — no subir sin verificar.
  meta: ^1.18.0

dev_dependencies:
  test: ^1.26.4
